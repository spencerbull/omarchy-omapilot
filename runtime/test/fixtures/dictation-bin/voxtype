#!/bin/sh
printf '%s\n' "$*" >> "$VOXTYPE_AUDIT"
if [ "$1" = "status" ]; then
  printf '%s\n' '{"alt":"idle"}'
  exit 0
fi
if [ "$1" = "record" ] && [ "$2" = "start" ]; then
  sleep 0.1
  exit 0
fi
if [ "$1" = "record" ] && { [ "$2" = "cancel" ] || [ "$2" = "stop" ]; }; then
  exit 0
fi
exit 1
